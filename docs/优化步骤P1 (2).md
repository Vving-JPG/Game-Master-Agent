# P1: 回归 Bug 紧急修复（v3 更新版）

> 优先级：🔴 最高 | 预估工作量：15 分钟 | 前置条件：无
> **v3 变更**: 本次更新引入了 3 个崩溃级回归 BUG，需立即修复

---

## Step 1.1: 修复 prompt_editor.py self._logger 不存在

**问题**: BUG-014 — 保存 Prompt 时 AttributeError

**文件**: `2workbench/presentation/editor/prompt_editor.py` 第 234 行

**修改方案**:

```python
# 修改前
self._logger.info("保存 Prompt: ...")

# 修改后
logger.info("保存 Prompt: ...")
```

**验收标准**:
- [ ] 保存 Prompt 不报 AttributeError
- [ ] 日志正确输出

---

## Step 1.2: 修复 runtime_panel.py QLabel 整数运算

**问题**: BUG-015 — 流式输出时 TypeError

**文件**: `2workbench/presentation/ops/debugger/runtime_panel.py` 第 113 行

**修改方案**:

```python
# 修改前
self._line_count += token.count('\n')

# 修改后
self._line_count_int += token.count('\n')
self._line_count.setText(f"行数: {self._line_count_int}")
```

注意：需要在 `__init__` 中初始化 `self._line_count_int = 0`。

**验收标准**:
- [ ] 流式输出时行数正确递增
- [ ] 不抛出 TypeError

---

## Step 1.3: 修复 runtime_panel.py _update_stats 方法不存在

**问题**: BUG-016 — 流式输出时 AttributeError

**文件**: `2workbench/presentation/ops/debugger/runtime_panel.py` 第 114 行

**修改方案**:

在 `ConsoleOutput` 类中添加 `_update_stats()` 方法：

```python
def _update_stats(self) -> None:
    """更新统计显示"""
    self._count_label.setText(f"字符: {self._count}")
    self._line_count.setText(f"行数: {self._line_count_int}")
```

**验收标准**:
- [ ] 流式输出后统计标签正确更新
- [ ] 不抛出 AttributeError

---

## 完成检查清单

- [ ] Step 1.1: prompt_editor self._logger 修复
- [ ] Step 1.2: runtime_panel QLabel 整数运算修复
- [ ] Step 1.3: runtime_panel _update_stats 方法添加
