# Game-Master-Agent 优化大纲（聚焦：编辑智能体）

> 基于最新源码深度审查 | 2026-05-03 (第三次更新)
>
> 核心目标：**让用户能在 IDE 中编辑智能体工作流，保存后能真正运行**

---

## 一、当前状态评估

### 1.1 编辑智能体 — 端到端流程

```
用户编辑图 → 保存 graph.json → 编译为 LangGraph → 注入 GMAgent → 运行 Agent → 运行时高亮
   ✅            ✅               ✅ 已修复          ✅ 已修复       ✅ 已修复      ✅ 已接线

用户编辑 Prompt → 保存到文件 → Agent 使用
   ✅              ✅ 已修复       ⚠️ 间接

用户管理工具 → Agent 调用工具 → 实际执行
   ✅            ✅ 已打通       ⚠️ 5/9 真实

用户点击运行 → GMAgent.run() → 显示结果
   ✅            ✅ 已实现      ✅ 已实现
```

### 1.2 各模块完成度

| 模块 | UI | 逻辑 | 数据持久化 | 与 Agent 连通 | 综合评价 |
|------|:--:|:----:|:----------:|:------------:|:--------:|
| 图编辑器 | ✅ | ✅ | ✅ | ✅ | **95%** |
| Prompt 编辑器 | ✅ | ✅ | ✅ | ✅ | **90%** |
| 工具管理器 | ✅ | ✅ | ✅ | ✅ | **80%** |
| Agent 运行控制 | ✅ | ✅ | — | ✅ | **90%** |
| GraphCompiler | — | ✅ | — | ✅ | **95%** |
| Feature 系统 | — | ✅ | ✅ | ✅ | **85%** |
| 运行时高亮 | ✅ | ✅ | — | ✅ | **95%** |
| GMAgent | — | ✅ | — | ✅ | **90%** |
| 运行时调试面板 | ✅ | ⚠️ 新Bug | — | ⚠️ | **70%** |

### 1.3 断裂点修复状态

#### ✅ 已修复（本轮集中修复 14 项）

| # | 断裂点 | 修复方式 |
|---|--------|----------|
| 1 | `nodes.py` 异步调用崩溃 | 全部改为 `async def` |
| 2 | GraphCompiler 条件边编译 Bug | 按 source 分组，每 source 只调用一次 |
| 3 | `_delete()` 未调用 `remove_node()` | 改为 `scene.remove_node()` |
| 4 | `_on_run_agent` 未使用项目图 | 调用 `compile_graph()` + `set_graph()` |
| 5 | Feature 系统未初始化 | `main.py` + `app.py` 注册 6 个 Feature |
| 6 | 缺少 langgraph 依赖 | pyproject.toml 新增 |
| 7 | TRPG 模板边缺少 condition | 补充 condition 字段 |
| 8 | Prompt 不持久化 | 调用 `project_manager.save_prompt()` |
| 9 | 工具测试面板模拟调用 | 真实调用 `t.invoke()` |
| 10 | ExplorationSystem 无 on_enable | 新增事件订阅 |
| 11 | ItemSystem 无 on_enable | 新增事件订阅 |
| 12 | 缺少拖拽连线 | 完整实现 |
| 13 | 节点 ID 不自动生成 | `_node_counter` + 唯一性检查 |
| 14 | 工具管理器与 tools.py 两套体系 | 统一通过 `register_tool()` + `get_all_tools()` |

#### ⚠️ 部分修复（1 项）

| # | 断裂点 | 状态 | 说明 |
|---|--------|:----:|------|
| 15 | `tools.py` 工具模拟实现 | ⚠️ | `update_player_stat`/`give_item`/`move_to_location` 已连接 Repository，但 `remove_item`/`update_npc_relationship`/`update_quest_status`/`store_memory` 仍为模拟 |

#### ❌ 新引入问题（3 项）

| # | 严重度 | 问题 | 文件 |
|---|--------|------|------|
| 16 | **P2** | `_update_stats()` 引用 `self._count_label` 未定义 | `runtime_panel.py` |
| 17 | P3 | `self._logger` 引用错误（应为模块级 `logger`） | `tool_manager.py` |
| 18 | P3 | `__import__()` 动态导入，可读性差 | `nodes.py:219` |

---

## 二、剩余优化项

### Phase 1 残留：新引入 Bug 修复
> 🟠 高 | 0.5 天

1. 修复 `runtime_panel.py` 中 `self._count_label` 未定义
2. 修复 `tool_manager.py` 中 `self._logger` → `logger`
3. 替换 `nodes.py` 中 `__import__()` 为顶层导入

### Phase 2 残留：工具系统完善
> 🟡 中 | 1-2 天

1. `remove_item` 连接 `ItemRepo`
2. `update_npc_relationship` 连接 `NPCRepo`
3. `update_quest_status` 连接 `QuestRepo`
4. `store_memory` 连接 `MemoryRepo`

### Phase 3：体验打磨（可选）
> 🟢 低 | 1-2 天

1. 调试面板运行控制打通（连接到 GMAgent）
2. 清理剩余 print 语句
3. 代码风格统一（ruff/mypy）

---

## 三、成功指标

| 指标 | 上次 | 当前 | 修复后 |
|------|------|------|--------|
| Agent 能否运行 | ⚠️ 崩溃 | ✅ | ✅ |
| 图编辑 → 编译 → 运行 | ❌ | ✅ | ✅ |
| 运行时节点高亮 | ✅ | ✅ | ✅ |
| Prompt 保存持久化 | ❌ | ✅ | ✅ |
| 工具真实执行 | 0/9 | 5/9 | 9/9 |
| Feature 系统工作 | ❌ | ✅ | ✅ |
| 拖拽连线 | ❌ | ✅ | ✅ |
