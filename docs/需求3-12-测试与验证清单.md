# 12 — 测试与验证清单

> 目标执行者：Trae AI
> 依赖：全部（00~11）
> 产出：无新文件（本文档为验证指南）

---

## 1. 单元测试

### 1.1 AITool 基类与注册中心（01）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.1.1 | AITool 实例化 | 创建继承 AITool 的测试工具 | 正常实例化，name/description/parameters 可访问 |
| 1.1.2 | to_llm_schema | 调用测试工具的 to_llm_schema() | 返回符合 OpenAI function calling 格式的 dict |
| 1.1.3 | ToolRegistry.register | 注册 3 个工具 | get_names() 返回 3 个名称 |
| 1.1.4 | ToolRegistry.register 重复 | 注册同名工具 | 不报错，覆盖旧工具 |
| 1.1.5 | ToolRegistry.get | 获取已注册工具 | 返回对应的 AITool 实例 |
| 1.1.6 | ToolRegistry.get 未注册 | 获取未注册工具 | 返回 None |
| 1.1.7 | ToolRegistry.to_llm_tools | 调用 to_llm_tools() | 返回列表，每个元素是 function schema |
| 1.1.8 | ToolRegistry.execute 正常 | 执行已注册工具 | 返回 ToolResult(success=True) |
| 1.1.9 | ToolRegistry.execute 未知 | 执行未注册工具名 | 返回 ToolResult(success=False, message 含 "未知工具") |
| 1.1.10 | ToolRegistry.execute 异常 | 工具抛出异常 | 返回 ToolResult(success=False, message 含异常信息) |

### 1.2 领域工具（02）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.2.1 | register_all_tools | 调用注册函数 | tool_registry 包含 14 个工具 |
| 1.2.2 | ReadProjectTool（无项目） | 未打开项目时执行 | success=False, message 含 "没有打开的项目" |
| 1.2.3 | ReadProjectTool（有项目） | 打开项目后执行 | success=True, data 含 prompts/skills/config |
| 1.2.4 | CreatePromptTool | 创建新提示词 | success=True, diff 非空, file_path 正确 |
| 1.2.5 | CreatePromptTool 文件已存在 | 创建同名提示词 | success=True, 覆盖旧文件 |
| 1.2.6 | EditPromptTool（不存在） | 修改不存在的提示词 | success=False, message 含 "不存在" |
| 1.2.7 | EditPromptTool（正常） | 修改存在的提示词 | success=True, diff 非空 |
| 1.2.8 | ListPromptsTool | 列出提示词 | success=True, data.prompts 为列表 |
| 1.2.9 | DeletePromptTool | 删除提示词 | success=True, 文件被删除 |
| 1.2.10 | CreateSkillTool | 创建新技能 | success=True, skills/{name}/SKILL.md 存在 |
| 1.2.11 | EditSkillTool | 修改技能 | success=True |
| 1.2.12 | ListSkillsTool | 列出技能 | success=True |
| 1.2.13 | DeleteSkillTool | 删除技能 | success=True, 目录被删除 |
| 1.2.14 | ReadGraphTool | 读取图定义 | success=True（有 graph.json 时） |
| 1.2.15 | UpdateGraphTool | 修改图定义 | success=True |
| 1.2.16 | ReadConfigTool | 读取配置 | success=True |
| 1.2.17 | UpdateConfigTool | 修改配置 | success=True |
| 1.2.18 | TestPromptTool | 测试提示词 | success=True, data.response 非空（需要 LLM 连接） |

### 1.3 数据模型（03）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.3.1 | PlanStep 创建 | 创建 PlanStep | 默认 status=PENDING |
| 1.3.2 | PlanStep.to_dict | 调用 to_dict() | 返回可序列化的 dict |
| 1.3.3 | ExecutionPlan 创建 | 创建含 3 步的计划 | steps 长度=3 |
| 1.3.4 | ExecutionPlan.to_dict | 调用 to_dict() | 返回完整 dict |
| 1.3.5 | ChatMessage 创建 | 创建消息 | role/content 正确 |
| 1.3.6 | ChatMessage.to_llm_message | 转换为 LLM 格式 | {"role": "user", "content": "..."} |
| 1.3.7 | SessionState 创建 | 创建会话 | session_id 非空 |

### 1.4 上下文收集器（03）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.4.1 | collect（无项目） | 未打开项目时收集 | 返回 {"error": "没有打开的项目"} |
| 1.4.2 | collect（有项目） | 打开项目后收集 | 返回含 prompts/skills/graph/config 的 dict |
| 1.4.3 | invalidate_cache | 清除缓存 | get_cached_context() 返回 None |
| 1.4.4 | _parse_yaml_frontmatter | 解析 Front Matter | 正确提取 key-value |

### 1.5 TaskPlanner（03）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.5.1 | plan（正常） | 发送 "创建一个测试提示词" | 返回 ExecutionPlan，steps 非空 |
| 1.5.2 | plan（无项目） | 未打开项目时规划 | 抛出 PlanParseError |
| 1.5.3 | _extract_json_block（代码块） | 输入含 ```json 的文本 | 正确提取 JSON |
| 1.5.4 | _extract_json_block（直接 JSON） | 输入纯 JSON 文本 | 正确提取 |
| 1.5.5 | _extract_json_block（无 JSON） | 输入纯文本 | 返回 None |
| 1.5.6 | refine_plan | 修改计划 | 返回新的 ExecutionPlan |

### 1.6 ToolExecutor（04）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.6.1 | execute_step（只读工具） | 执行 list_prompts | status=COMPLETED |
| 1.6.2 | execute_step（写工具） | 执行 create_prompt | status=COMPLETED, diff 非空 |
| 1.6.3 | execute_step（失败） | 执行不存在的工具 | status=FAILED |
| 1.6.4 | reject_step | 拒绝已完成的步骤 | 文件恢复到原始内容 |
| 1.6.5 | SnapshotManager 快照/恢复 | 创建→修改→恢复 | 文件内容恢复 |
| 1.6.6 | DiffGenerator | 生成 diff | diff 格式正确 |

### 1.7 AIAssistantService（05）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 1.7.1 | initialize | 初始化服务 | 工具已注册，事件已订阅 |
| 1.7.2 | state 初始值 | 检查初始状态 | state=IDLE |
| 1.7.3 | new_session | 新建会话 | session_id 变化，历史清空 |
| 1.7.4 | get_session_history | 获取历史 | 返回消息列表 |

---

## 2. 集成测试

### 2.1 EventBus 通信

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 2.1.1 | 用户消息 → 服务 | 发送 ai_assistant.user_message 事件 | 服务收到并开始处理 |
| 2.1.2 | 服务 → AI 响应 | 服务发送 ai_assistant.response 事件 | UI 显示响应消息 |
| 2.1.3 | 计划就绪 | 服务发送 ai_assistant.plan_ready 事件 | UI 显示 PlanPanel |
| 2.1.4 | 计划操作 | UI 发送 ai_assistant.plan_action 事件 | 服务执行对应操作 |
| 2.1.5 | 步骤完成 | 服务发送 ai_assistant.step_completed 事件 | PlanPanel 更新步骤状态 |
| 2.1.6 | 执行完成 | 服务发送 ai_assistant.execution_finished 事件 | UI 显示摘要 |

### 2.2 完整流程测试

| # | 测试场景 | 操作步骤 | 预期结果 |
|---|----------|----------|----------|
| 2.2.1 | 闲聊流程 | 输入 "什么是好的场景提示词？" | AI 直接回答，不生成计划 |
| 2.2.2 | 创建提示词流程 | 输入 "创建一个暗黑风格的地下城探索场景" | 生成计划 → 显示 PlanPanel → 确认 → 执行 → Diff 预览 → 确认 → 文件创建成功 |
| 2.2.3 | 创建技能流程 | 输入 "创建一个陷阱检测技能" | 生成计划 → 执行 → skills/trap_detection/SKILL.md 创建成功 |
| 2.2.4 | 修改流程 | 输入 "修改 system 提示词，添加暗黑风格描述" | 生成计划 → 执行 → system.md 被修改 |
| 2.2.5 | 删除流程 | 输入 "删除 test_prompt 提示词" | 生成计划 → 执行 → 文件被删除 |
| 2.2.6 | 多步骤流程 | 输入 "创建地下城场景和对应的陷阱检测技能" | 计划含 3+ 步骤 → 逐步确认执行 |
| 2.2.7 | 一键确认 | 生成计划后点击 "确认全部" | 所有步骤自动执行 |
| 2.2.8 | 修改计划 | 生成计划后输入修改意见 | 计划更新，显示新计划 |
| 2.2.9 | 取消计划 | 生成计划后点击取消 | 计划取消，回到空闲状态 |
| 2.2.10 | 拒绝变更 | 执行步骤后拒绝 Diff | 文件恢复到原始内容 |
| 2.2.11 | 跳过步骤 | 执行中跳过某个步骤 | 步骤标记为 SKIPPED，继续下一步 |
| 2.2.12 | 新建会话 | 点击新会话按钮 | 消息清空，计划清除 |

---

## 3. UI 测试

### 3.1 AIAssistantPanel（06）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 3.1.1 | 面板显示 | 切换到 AI 助手标签页 | 显示消息列表 + 输入框 + 发送按钮 |
| 3.1.2 | 发送消息 | 输入文本 + 点击发送 | 消息出现在列表中，输入框清空 |
| 3.1.3 | Enter 发送 | 输入文本 + 按 Enter | 消息发送 |
| 3.1.4 | Shift+Enter 换行 | 按 Shift+Enter | 输入框换行，不发送 |
| 3.1.5 | 空消息 | 不输入内容 + 点击发送 | 不发送，无反应 |
| 3.1.6 | 思考指示器 | AI 处理中 | 显示 "AI 正在思考..." 动画 |
| 3.1.7 | 自动滚动 | 消息超出可视区域 | 自动滚动到底部 |
| 3.1.8 | 新会话 | 点击新会话按钮 | 消息清空 |

### 3.2 PlanPanel（07）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 3.2.1 | 计划显示 | 计划就绪后 | 显示步骤列表，每个步骤有图标+描述+工具名 |
| 3.2.2 | 展开详情 | 点击步骤的展开按钮 | 显示参数详情 |
| 3.2.3 | 确认全部 | 点击 "确认全部执行" | plan_action 信号发出，action="confirm_all" |
| 3.2.4 | 修改计划 | 点击 "修改计划" | 显示修改输入框 |
| 3.2.5 | 提交修改 | 输入修改意见 + 提交 | plan_action 信号发出，action="modify" |
| 3.2.6 | 取消修改 | 点击取消修改 | 输入框隐藏 |
| 3.2.7 | 取消计划 | 点击 "取消" | plan_action 信号发出，action="cancel" |
| 3.2.8 | 状态更新 | 步骤完成 | 图标变为 ✅，背景变绿 |

### 3.3 DiffViewer（08）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 3.3.1 | Diff 显示 | 步骤完成后 | 显示 diff 文本，新增行绿色，删除行红色 |
| 3.3.2 | 统计信息 | 显示 diff | 显示 "+N 行新增" 和 "-N 行删除" |
| 3.3.3 | 确认变更 | 点击 "确认变更" | diff_action 信号发出，action="confirm" |
| 3.3.4 | 拒绝变更 | 点击 "拒绝变更" | diff_action 信号发出，action="reject" |
| 3.3.5 | 文件路径 | 显示 diff | 显示被修改的文件路径 |

### 3.4 主窗口集成（09）

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 3.4.1 | 标签页存在 | 启动应用 | CenterPanel 中有 "🤖 AI 助手" 标签页 |
| 3.4.2 | 菜单入口 | 打开 Agent 菜单 | 有 "🤖 AI 助手" 菜单项 |
| 3.4.3 | 快捷键 | 按 Ctrl+Shift+A | 切换到 AI 助手标签页 |
| 3.4.4 | 标签页切换 | 在不同标签页间切换 | AI 助手面板正常显示/隐藏 |

---

## 4. 边界情况测试

| # | 测试场景 | 操作 | 预期结果 |
|---|----------|------|----------|
| 4.1 | 无项目时使用 | 不打开项目，直接使用 AI 助手 | 工具执行返回 "没有打开的项目" |
| 4.2 | 项目切换 | 使用 AI 助手过程中切换项目 | 会话重置，上下文更新 |
| 4.3 | 项目关闭 | 使用 AI 助手过程中关闭项目 | 会话清理，快照清除 |
| 4.4 | LLM 连接失败 | 断网状态下使用 | 显示错误消息，不崩溃 |
| 4.5 | LLM 返回非 JSON | LLM 返回纯文本而非 JSON 计划 | PlanParseError，显示友好错误 |
| 4.6 | LLM 返回无效 JSON | JSON 格式错误 | PlanParseError，显示解析错误 |
| 4.7 | LLM 返回空计划 | plan 数组为空 | 提示用户更详细描述需求 |
| 4.8 | 超长内容 | 创建超长提示词（>10000 字符） | 正常处理，不截断 |
| 4.9 | 特殊字符 | 提示词中包含 JSON 特殊字符 | 正确转义，不破坏 JSON 解析 |
| 4.10 | 并发操作 | 快速连续发送多条消息 | 消息按序处理，不丢失 |
| 4.11 | 中文文件名 | 创建含中文的提示词名 | 正常处理（但建议使用英文名） |
| 4.12 | 重复创建 | 创建已存在的提示词 | 覆盖旧文件，生成 diff |

---

## 5. 性能测试

| # | 测试项 | 操作 | 预期结果 |
|---|--------|------|----------|
| 5.1 | 大量提示词 | 项目中有 50+ 提示词时使用 AI 助手 | 上下文收集 < 2s，UI 不卡顿 |
| 5.2 | 大量技能 | 项目中有 30+ 技能时使用 AI 助手 | 上下文收集 < 2s |
| 5.3 | 长对话 | 进行 20+ 轮对话 | 内存不泄漏，UI 响应正常 |
| 5.4 | Diff 大文件 | 修改大文件（>500 行）的 diff 显示 | 渲染 < 1s，滚动流畅 |

---

## 6. 执行顺序

建议按以下顺序进行测试：

```
阶段 1：单元测试（不依赖 UI）
  ├── 1.1 AITool 基类
  ├── 1.2 领域工具
  ├── 1.3 数据模型
  ├── 1.4 上下文收集器
  ├── 1.5 TaskPlanner（需要 LLM 连接）
  ├── 1.6 ToolExecutor
  └── 1.7 AIAssistantService

阶段 2：集成测试（需要完整环境）
  ├── 2.1 EventBus 通信
  └── 2.2 完整流程测试

阶段 3：UI 测试（需要启动应用）
  ├── 3.1 AIAssistantPanel
  ├── 3.2 PlanPanel
  ├── 3.3 DiffViewer
  └── 3.4 主窗口集成

阶段 4：边界情况 + 性能
  ├── 4.x 边界情况
  └── 5.x 性能测试
```

---

## 7. 快速验证脚本

完成所有文件创建后，运行以下快速验证：

```python
"""快速验证脚本 — 在 Python 控制台中运行"""
import sys
sys.path.insert(0, "/path/to/2workbench")

# 1. 导入检查
print("=== 导入检查 ===")
from feature.services.ai_assistant.tools import tool_registry, register_all_tools
from feature.services.ai_assistant.models import PlanStep, ExecutionPlan, SessionState
from feature.services.ai_assistant.context import context_collector
from feature.services.ai_assistant.planner import task_planner
from feature.services.ai_assistant.executor import tool_executor
from feature.services.ai_assistant.service import ai_assistant_service
from feature.services.ai_assistant.prompts import PLANNER_SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT
print("✅ 所有模块导入成功")

# 2. 工具注册
print("\n=== 工具注册 ===")
register_all_tools()
names = tool_registry.get_names()
print(f"✅ 已注册 {len(names)} 个工具: {names}")
assert len(names) == 14, f"预期 14 个工具，实际 {len(names)}"

# 3. LLM Schema 生成
print("\n=== LLM Schema ===")
schemas = tool_registry.to_llm_tools()
print(f"✅ 生成 {len(schemas)} 个工具 schema")
for schema in schemas:
    name = schema["function"]["name"]
    params = list(schema["function"]["parameters"]["properties"].keys())
    print(f"  {name}: {params}")

# 4. 数据模型
print("\n=== 数据模型 ===")
step = PlanStep(step_id=1, tool_name="test", description="测试", parameters={})
assert step.status.value == "pending"
plan = ExecutionPlan(goal="测试", steps=[step])
assert plan.to_dict()["steps"][0]["tool_name"] == "test"
print("✅ 数据模型正常")

# 5. 提示词模板
print("\n=== 提示词模板 ===")
assert "{project_context}" in PLANNER_SYSTEM_PROMPT
formatted = PLANNER_SYSTEM_PROMPT.format(project_context='{"test": true}')
assert "TRPG" in formatted
print("✅ 提示词模板正常")

print("\n🎉 所有快速验证通过！")
```
