# 选择界面重构指导文档 — 仿 Godot 4.6 项目管理器

> 目标：将当前的项目选择器重构为与截图一致的 Godot 4.6 项目管理器界面
> 执行者：Trae AI
> 涉及文件：
>   - `2workbench/presentation/dialogs/project_selector.py`（完全重写）
>   - `2workbench/presentation/project/new_dialog.py`（完全重写）

---

## 一、Godot 4.6 项目管理器精确 UI 规格

### 1.1 整体窗口结构（从上到下 4 层）

```
┌──────────────────────────────────────────────────────────────┐
│ 🤖 GODOT    [项目] [资产库]                    [设置]        │  ← Header (~64px)
├──────────────────────────────────────────────────────────────┤
│ [+ 创建] [导入] [扫描]  [筛选项目___]   🔍 排序: [最近编辑▼] │  ← Toolbar (~40px)
├────────────────────────────────────────────────┬─────────────┤
│                                                │  ✏️ 编辑    │
│  ☆ [🟦] RPG                    4.6  2026-04-26  │  ▶ 运行    │
│      📁 D:/RPG/RPG                            │  📝 重命名  │
│                                                │  📄 创建副本│
│  ☆ [🟦] Chapter of Immortality  4.6  2026-03  │  🏷️ 管理标签│
│      📁 D:/Chapter of Immortality              │  🗑️ 移除    │
│                                                │  🔗 移除缺失│
│                                                │             │
│                                                │  ❤️ 捐赠    │
├────────────────────────────────────────────────┴─────────────┤
│                              v4.6.2.stable [71f334935]       │  ← Footer (~28px)
└──────────────────────────────────────────────────────────────┘
```

### 1.2 颜色规格

| 元素 | 颜色 | Hex |
|------|------|-----|
| 窗口背景 | 深炭色 | `#1E1E1E` |
| Header 背景 | 近黑 | `#151515` |
| Toolbar 背景 | 深灰 | `#2A2A2A` |
| 右侧操作栏背景 | 中深灰 | `#252525` |
| Footer 背景 | 最深灰 | `#181818` |
| 主文字 | 白色 | `#FFFFFF` |
| 次要文字 | 浅灰 | `#AAAAAA` |
| 三级文字 | 中灰 | `#888888` |
| 禁用文字 | 深灰 | `#666666` |
| 强调蓝（选中标签） | 亮蓝 | `#4A90D9` |
| 项目图标蓝 | 中蓝 | `#5B9BD5` |

**注意**：以上颜色为 Godot 默认暗色主题。我们的实现必须从 `theme_manager.PALETTES` 获取对应语义的颜色变量，不要硬编码。

### 1.3 字体规格

| 用途 | 大小 | 粗细 | 颜色 |
|------|------|------|------|
| Logo 文字 | 16px | Bold | 白 |
| 导航标签 | 13px | Medium | 蓝/灰 |
| Toolbar 按钮 | 12px | Regular | 白 |
| 项目名称 | 14px | Medium | 白 |
| 项目元数据 | 11px | Regular | 灰 |
| 操作栏按钮 | 12px | Regular | 白/浅灰 |
| 版本号 | 10px | Regular | 深灰 |

### 1.4 间距系统（8px 网格）

- Header 高度：64px
- Toolbar 高度：40px
- 项目行高：72px
- 操作栏按钮高：40px
- Footer 高度：28px
- 内边距统一：8px 的倍数

---

## 二、project_selector.py 完全重写

### 2.1 精确布局结构

```python
QDialog (960x640, 无边框或标准边框)
└── QVBoxLayout (spacing=0, margin=0)
    │
    ├── #header QFrame (固定高度 64px)
    │   └── QHBoxLayout (margin: 0 12px)
    │       ├── QLabel "🎲" (Logo emoji, 24px)           # 左
    │       ├── QLabel "Game Master Agent" (16px bold)    # 左
    │       ├── QSpacerItem (stretch=1)                    # 中
    │       ├── QPushButton "📋 项目" (active, blue)       # 中（导航标签）
    │       ├── QPushButton "📦 资产库" (inactive, gray)   # 中
    │       └── QSpacerItem (stretch=1)                    # 中
    │       # 注意：Godot 右侧有"设置"，我们可以省略或保留
    │
    ├── #toolbar QFrame (固定高度 40px)
    │   └── QHBoxLayout (margin: 0 12px)
    │       ├── QPushButton "+ 新建" #newBtn              # 左
    │       ├── QPushButton "📂 导入" #importBtn           # 左
    │       ├── QPushButton "🔍 扫描" #scanBtn             # 左
    │       ├── QLineEdit "筛选项目..." #filterEdit        # 中左
    │       ├── QSpacerItem (stretch=1)                    # 中
    │       ├── QLabel "排序:"                             # 右
    │       └── QComboBox ["最近编辑" ▼] #sortCombo        # 右
    │
    ├── #body QHBoxLayout (stretch=1, spacing=0)
    │   ├── #projectListPanel QWidget (stretch=1)          # 左侧 ~78%
    │   │   └── QVBoxLayout (margin= 8px 12px)
    │   │       └── QListWidget #projectList
    │   │           # 每个 item 高 72px，自定义 delegate 绘制
    │   │           # 左侧：☆ 收藏 + 48x48 图标 + 名称/路径
    │   │           # 右侧：版本 + 修改时间
    │   │
    │   └── #actionPanel QFrame (固定宽度 180px)           # 右侧 ~22%
    │       └── QVBoxLayout (margin: 8px, spacing=1px)
    │           ├── QPushButton "✏️ 编辑" #editBtn
    │           ├── QPushButton "▶ 运行" #runBtn (primary)
    │           ├── QPushButton "📝 重命名" #renameBtn
    │           ├── QPushButton "📄 创建副本" #duplicateBtn
    │           ├── QPushButton "🏷️ 管理标签" #tagBtn
    │           ├── QFrame (分割线, 1px)
    │           ├── QPushButton "🗑️ 移除" #removeBtn
    │           ├── QPushButton "🔗 移除缺失项" #missingBtn (disabled)
    │           ├── QSpacerItem (stretch=1)
    │           └── QPushButton "❤️ 捐赠" #donateBtn (可选，可改为"关于")
    │
    └── #footer QFrame (固定高度 28px)
        └── QHBoxLayout
            ├── QSpacerItem (stretch=1)
            └── QLabel "v2.0.0" (10px, 灰色, 靠右)
```

### 2.2 项目列表自定义绘制（QListWidget + 自定义 ItemDelegate）

Godot 的项目列表不是简单的文本列表，每个 item 包含多行信息。使用 `QStyledItemDelegate` 自定义绘制：

```python
from PyQt6.QtWidgets import QStyledItemDelegate, QStyle

class ProjectItemDelegate(QStyledItemDelegate):
    """自定义项目列表项绘制 — 仿 Godot 风格"""

    def __init__(self, parent=None):
        super().__init__(parent)

    def paint(self, painter, option, index):
        """绘制项目项"""
        painter.save()
        info = index.data(Qt.ItemDataRole.UserRole)
        if not info:
            painter.restore()
            return

        # 获取主题颜色
        try:
            from presentation.theme.manager import theme_manager
            p = theme_manager.PALETTES.get(theme_manager.current_theme, {})
        except Exception:
            p = {}

        bg_primary = p.get("bg_primary", "#1e1e1e")
        bg_hover = p.get("bg_hover", "#2a2a2a")
        bg_selected = p.get("accent", "#007acc")
        text_bright = p.get("text_bright", "#ffffff")
        text_secondary = p.get("text_secondary", "#858585")
        text_disabled = p.get("text_disabled", "#666666")
        border = p.get("border", "#3e3e42")

        rect = option.rect

        # 背景
        if option.state & QStyle.StateFlag.State_Selected:
            painter.fillRect(rect, QColor(bg_selected))
        elif option.state & QStyle.StateFlag.State_MouseOver:
            painter.fillRect(rect, QColor(bg_hover))
        else:
            painter.fillRect(rect, QColor(bg_primary))

        # 底部分割线
        painter.setPen(QPen(QColor(border), 1))
        painter.drawLine(rect.left() + 12, rect.bottom(), rect.right() - 12, rect.bottom())

        # 图标区域（48x48，左侧）
        icon_rect = QRect(rect.left() + 44, rect.top() + 12, 48, 48)
        template = info.get("template", "blank")
        icon_colors = {"blank": "#569cd6", "trpg": "#4ec9b0", "chatbot": "#c586c0"}
        icon_color = QColor(icon_colors.get(template, "#569cd6"))
        painter.setBrush(QBrush(icon_color))
        painter.setPen(Qt.PenStyle.NoPen)
        painter.drawRoundedRect(icon_rect, 6, 6)

        # 图标上的 emoji
        icons = {"blank": "📄", "trpg": "🎲", "chatbot": "🤖"}
        painter.setFont(QFont("", 20))
        painter.setPen(QColor(text_bright))
        painter.drawText(icon_rect, Qt.AlignmentFlag.AlignCenter, icons.get(template, "📄"))

        # 项目名称（图标右侧）
        name_rect = QRect(rect.left() + 104, rect.top() + 14, rect.width() - 200, 24)
        painter.setFont(QFont("", 14, QFont.Weight.Medium))
        painter.setPen(QColor(text_bright))
        painter.drawText(name_rect, Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,
                        info.get("name", ""))

        # 路径（名称下方）
        path_text = f"📁 {info.get('path', '')}"
        path_rect = QRect(rect.left() + 104, rect.top() + 40, rect.width() - 200, 20)
        painter.setFont(QFont("", 11))
        painter.setPen(QColor(text_secondary))
        painter.drawText(path_rect, Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,
                        path_text)

        # 右侧：修改时间
        time_rect = QRect(rect.right() - 180, rect.top() + 20, 170, 20)
        painter.setFont(QFont("", 11))
        painter.setPen(QColor(text_disabled))
        painter.drawText(time_rect, Qt.AlignmentFlag.AlignRight | Qt.AlignmentFlag.AlignVCenter,
                        info.get("modified", ""))

        painter.restore()

    def sizeHint(self, option, index):
        return QSize(0, 72)  # 每行 72px 高
```

### 2.3 右侧操作栏按钮样式

```python
def _create_action_button(self, icon: str, text: str, object_name: str = "",
                          primary: bool = False, danger: bool = False) -> QPushButton:
    """创建操作栏按钮"""
    btn = QPushButton(f"  {icon}  {text}")
    btn.setObjectName(object_name)
    btn.setCursor(Qt.CursorShape.PointingHandCursor)
    btn.setMinimumHeight(40)
    btn.setFlat(True)

    # 从主题获取颜色
    try:
        from presentation.theme.manager import theme_manager
        p = theme_manager.PALETTES.get(theme_manager.current_theme, {})
    except Exception:
        p = {}

    bg_panel = p.get("bg_secondary", "#252526")
    text_primary = p.get("text_primary", "#cccccc")
    text_bright = p.get("text_bright", "#ffffff")
    accent = p.get("accent", "#007acc")
    error = p.get("error", "#f44747")
    bg_hover = p.get("bg_hover", "#3e3e42")

    if primary:
        color = accent
    elif danger:
        color = error
    else:
        color = text_primary

    btn.setStyleSheet(f"""
        QPushButton#{object_name} {{
            background-color: transparent;
            color: {color};
            border: none;
            padding: 8px 12px;
            text-align: left;
            font-size: 12px;
            border-radius: 4px;
        }}
        QPushButton#{object_name}:hover {{
            background-color: {bg_hover};
        }}
    """)
    return btn
```

### 2.4 Toolbar 按钮样式

```python
def _create_toolbar_button(self, icon: str, text: str) -> QPushButton:
    """创建工具栏按钮"""
    btn = QPushButton(f"{icon} {text}")
    btn.setCursor(Qt.CursorShape.PointingHandCursor)
    btn.setFixedHeight(30)

    try:
        from presentation.theme.manager import theme_manager
        p = theme_manager.PALETTES.get(theme_manager.current_theme, {})
    except Exception:
        p = {}

    bg_toolbar = p.get("bg_secondary", "#252526")
    text_primary = p.get("text_primary", "#cccccc")
    border = p.get("border", "#3e3e42")
    bg_hover = p.get("bg_hover", "#3e3e42")

    btn.setStyleSheet(f"""
        QPushButton {{
            background-color: {bg_toolbar};
            color: {text_primary};
            border: 1px solid {border};
            border-radius: 4px;
            padding: 4px 12px;
            font-size: 12px;
        }}
        QPushButton:hover {{
            background-color: {bg_hover};
        }}
    """)
    return btn
```

### 2.5 排序功能

```python
self._sort_combo.addItems(["最近编辑", "项目名称", "创建时间"])

def _on_sort_changed(self, text: str) -> None:
    if text == "项目名称":
        self._projects.sort(key=lambda x: x.get("name", ""))
    elif text == "创建时间":
        self._projects.sort(key=lambda x: x.get("created", ""))
    else:  # 最近编辑（默认）
        self._projects.sort(key=lambda x: x.get("modified", ""), reverse=True)
    self._refresh_list()
```

### 2.6 交互行为

| 操作 | 行为 |
|------|------|
| 单击项目 | 高亮选中，右侧操作栏按钮启用 |
| 双击项目 | 直接运行（emit + accept） |
| 搜索/筛选 | 实时过滤项目列表 |
| 排序切换 | 重新排列项目列表 |
| 点击"运行" | emit project_selected + accept |
| 点击"编辑" | emit project_selected + accept（同运行） |
| 点击"移除" | 确认对话框 → 删除项目 → 刷新列表 |
| 点击"新建" | emit new_project_requested + accept |
| 点击"导入" | QFileDialog.getExistingDirectory → 打开项目 |
| 无项目选中时 | 右侧操作栏按钮 disabled |

---

## 三、new_dialog.py 完全重写

### 3.1 布局结构

```
QDialog (560x500)
└── QVBoxLayout (spacing=16, margin=32)
    ├── QLabel "创建新项目" (18px bold)
    │
    ├── QLabel "项目名称" (12px, gray)
    ├── QLineEdit #nameEdit (placeholder: "my_agent", 36px高)
    │
    ├── QLabel "项目路径" (12px, gray)
    ├── QHBoxLayout
    │   ├── QLineEdit #pathEdit (只读, 显示路径)
    │   └── QPushButton "浏览..." #browseBtn (80px宽)
    │
    ├── QLabel "选择模板" (12px, gray)
    ├── QHBoxLayout (模板卡片, spacing=16)
    │   ├── TemplateCard "空白项目" (📄, 蓝色)
    │   ├── TemplateCard "TRPG 游戏" (🎲, 绿色)
    │   └── TemplateCard "对话机器人" (🤖, 紫色)
    │
    ├── QLabel #descLabel (模板描述, 自动更新)
    │
    ├── QLabel "项目描述（可选）" (12px, gray)
    ├── QLineEdit #descEdit (placeholder: "简要描述你的项目...")
    │
    ├── QSpacerItem (stretch=1)
    │
    └── QHBoxLayout
        ├── QPushButton "取消" #cancelBtn
        ├── QSpacerItem
        └── QPushButton "创建并运行" #createBtn (primary)
```

### 3.2 关键修改：路径选择

```python
def _on_browse_path(self) -> None:
    """浏览选择项目保存位置"""
    path = QFileDialog.getExistingDirectory(
        self, "选择项目保存位置",
        str(self._project_dir),
    )
    if path:
        self._project_dir = Path(path)
        self._path_edit.setText(path)
```

### 3.3 get_project_data 新增 directory

```python
def get_project_data(self) -> dict:
    return {
        "name": self._name_edit.text().strip(),
        "template": self._selected_template,
        "description": self._desc_edit.text().strip(),
        "directory": str(self._project_dir),  # 新增
    }
```

### 3.4 修复模板描述 bug

当前第 337 行 `if 'p' in dir()` 永远为 True，改为直接使用变量：

```python
# 修改前
background-color: {p.get('bg_secondary', '#252526') if 'p' in dir() else '#252526'};

# 修改后
background-color: {bg_secondary};
```

---

## 四、app.py 适配

调用方需要适配 `get_project_data()` 新增的 `directory` 字段：

```python
# app.py 中 _on_new_project 或类似方法
data = dialog.get_project_data()
if not data["name"]:
    self._show_message("项目名称不能为空")
    return

# 使用用户选择的路径，而非默认路径
project_dir = Path(data.get("directory", str(project_manager.workspace_dir / "data")))
path = project_manager.create_project(
    name=data["name"],
    template=data["template"],
    directory=str(project_dir),
)
```

---

## 五、注意事项

1. **不要使用 print**：全部使用 `logger`
2. **不要使用 self._logger**：使用模块级 `logger`（历史 BUG，已出现 3 次）
3. **不要硬编码颜色**：全部从 `theme_manager.PALETTES` 获取
4. **保留 refresh_theme() 方法**：外部主题切换时调用
5. **保留信号接口**：`project_selected(str)`, `new_project_requested()`
6. **QListWidget 使用自定义 ItemDelegate**：不要用默认的文本 item
7. **项目 item 高度固定 72px**：通过 `sizeHint` 返回
8. **图标使用 emoji**：📄（空白）、🎲（TRPG）、🤖（对话机器人）
9. **操作栏按钮使用 setFlat(True)**：无边框，hover 时显示背景
10. **排序默认"最近编辑"**：按修改时间倒序
